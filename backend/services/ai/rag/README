# build database (one-time)
python ingest.py

# Serve
uvicorn main:app --host 0.0.0.0 --port 8000

# Query
GET /search?q=office+cleaning&limit=5
POST /search-by-url  {"url": "https://firma-budowlana.pl", "limit": 10}            //BODY W JSONIE!
GET /health